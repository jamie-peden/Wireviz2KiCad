# Wireviz2KiCad
WireViz → KiCad converter  This project converts a WireViz YAML (diagram metadata) plus PNG and BOM assets into a KiCad schematic sheet (`.kicad_sch`) embedding the diagram, title block and a native KiCad-like BOM table.
